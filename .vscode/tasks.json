{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C++ project",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "make -f x86_64_with_debugger.mk install_hana && make -f x86_64_with_debugger.mk",
        }
    ]
}
